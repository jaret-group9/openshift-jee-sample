language: java
jdk: oraclejdk8

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - mvn test -B

cache:
  directories:
  - $HOME/.m2

deploy:
  provider: openshift
  user: "jelle.aret@group9.nl"
  password: "@Group901"
  domain: jee-sample
  app: travis-sample-app